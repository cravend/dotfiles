- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.aliases: aliases
    ~/.gitconfig: gitconfig
    ~/.prettierrc: prettierrc
    ~/.vimrc: vimrc
    ~/.zshrc_default: zshrc
